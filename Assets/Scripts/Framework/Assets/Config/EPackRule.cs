namespace CoinFlip.Assets
{
    /// <summary>YooAsset-aligned bundle pack rules (Strategy).</summary>
    public enum EPackRule
    {
        PackSeparately = 0,
        PackDirectory = 1,
        PackTopDirectory = 2,
        PackCollector = 3,
        PackGroup = 4,
        PackRawFile = 5,
    }
}
